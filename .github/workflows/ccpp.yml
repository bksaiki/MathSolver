name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Build
      run: |
        mkdir build/test
        mkdir build/types
        make build build-tests
    - name: Testing
      run: make tests
